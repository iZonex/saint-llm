# ADR-NNNN: <Title>

- **Status**: proposed | accepted | rejected | deferred | superseded
- **Date**: YYYY-MM-DD
- **Deciders**: <names>
- **Supersedes**: ADR-NNNN (if any)
- **Related**: AUGMENTATIONS rows (e.g. RL-01), other ADRs

## Context

What's the situation that needs a decision? What constraints apply (compute,
time, scope)? What's the current state of the world (frontier practice, our
codebase, prior decisions)? Cite sources for any claims about external state.

## Decision

What we're going to do, in 1-3 short sentences. Be concrete. If this is a
design choice, name the chosen option and the rejected ones.

## Consequences

What changes as a result. Both intended (the win we're chasing) and
unintended (debt we're taking on, things now harder, follow-up ADRs needed).
Be honest — if we're trading X for Y, name X.

## Alternatives considered

Bullet list of other options we evaluated, with one-line reasons each was
rejected. If an alternative is also viable but deferred, mark it.

## Implementation notes (optional)

Hooks: what files / packages / version this lands in. Out-of-scope items
that the implementer must NOT slip in.
